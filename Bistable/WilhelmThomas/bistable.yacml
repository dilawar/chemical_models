compartment compt {

    volume = 1;

    // Species in system
    S [ 
        conc = "20*sin(45)"
        , plot = true
        //conc = "(t > 10 && t < 15) ? 1:2", plot = conc 
    ];

    X [ conc = 10, plot = conc ];

    Y [ conc = 5, plot = conc ];

    P [ conc = 0, plot = conc ];

    // k1 = 8, k2 = 1, k3 = 1, k4 = 1.5
    r1 [ kf = 8, kb = 2 ];
    X, S -> r1 -> P;

    //solver = ksolve
    dt = 0.001
    sim_time = 10
}

